opam-version: "1.2"

depends: [
  "bisect_ppx" {build & >= "1.3.0"}
  "jbuilder" {build & = "1.0+beta14"}

  "base" {test}
  "core_bench" {test}
  "qcheck" {test & >= "0.7"}
]

available: [ocaml-version >= "4.04.2"]
